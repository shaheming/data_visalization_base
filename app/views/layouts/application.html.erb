<!DOCTYPE html>
<html>
	<head>
		<title>Heming Sha</title>
		<%= csrf_meta_tags %>
		<%= stylesheet_link_tag 'application' %>
		
	</head>
	<body>
		<div class="container-fluid">
			<%= render "common/navbar" %>
			<%= render "common/flashes" %>
			<%= yield %>
		</div>
		
		<%= render "common/footer" %>
		<%= javascript_include_tag 'application' %>
		<%= yield :handwrite_javascript %>
	</body>
</html>